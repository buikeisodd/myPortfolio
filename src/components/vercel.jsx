import { Triangle } from 'lucide-react';

export const VercelIcon = () => {
  return(
    <div>
      {/* Default Vercel style: Solid Black Triangle */}
      <Triangle size={32} fill="currentColor" strokeWidth={0} />
    </div>
  );
};

